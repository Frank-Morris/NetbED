---
- name: Configure web server
  hosts: all
  become: true
  tasks:
    - name: Update apt package cache
      ansible.builtin.apt:
        update_cache: true

    - name: Install Apache web server
      ansible.builtin.apt:
        name: apache2
        state: present
